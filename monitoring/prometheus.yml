global:
  scrape_interval: 15s # 15초마다 메트릭을 수집

scrape_configs:
  - job_name: 'uket-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # host.docker.internal 대신, 서비스 이름을 직접 사용!
      - targets: ['uket-app:8080']
